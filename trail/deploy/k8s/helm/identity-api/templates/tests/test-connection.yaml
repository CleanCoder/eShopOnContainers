apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "standard-chart.fullname" . }}-test-connection"
  labels:
    {{- include "standard-chart.common.metadataLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "standard-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
